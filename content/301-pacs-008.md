---
title: "pacs.008: The Message That Actually Moves the Money"
level: 300
category: Message Deep Dives
summary: "The interbank workhorse. When one bank tells another 'pay this customer, here's who, here's how much,' it sends a pacs.008. We open it field by field, then let you edit a live one."
minutes: 11
updated: 2026-07-13
tags: [pacs.008, credit transfer, CBPR+, FINplus]
related: [101-nostro-vostro, 302-pain-001, 302-pacs-family, 310-status-reports]
earnedSkill: "Read a pacs.008 top to bottom, name what each block is for, keep MsgId/EndToEndId/UETR straight, and spot the fields that get payments rejected in the real world."
num: 308
status: published
---

> **The problem first.** Bob's bank has accepted his instruction. The money is leaving Dubai for Bangalore. But the *next* bank in the chain has never seen Bob, never seen this payment, and won't take a phone call about it.

This is the one message to learn in depth if you only ever learn one. And the way to really read it is to first work out what the situation *forces* into it.

## What has to be in the message?

{{think}}
A machine at the next bank — another country, another system — has to act on this payment with zero ambiguity and no human involved. It's never heard of Bob. It can't ask a follow-up question.

Given all that, what does Bob's bank have to put in the message for that machine to process it safely and pass it on?
{{reveal}}
Everything that removes ambiguity:

- **Every party named unmistakably** — not "Bob's bank" but a `BIC`, a code that resolves to exactly one institution anywhere on earth.
- **The exact amount and currency** that settles between the banks.
- **References that survive every hop** — so the payment can be tracked and reconciled across the chain.
- **A timestamp with a timezone**, so "when" is unambiguous across borders.
- All of it in a **fixed, schema-checkable structure**, so a machine can validate it before acting.

That message is the **pacs.008** — the ISO 20022 successor to the MT103, and by volume one of the most important messages in global finance.
{{/think}}

## What the name means, and who sends it

Decode the name once and it's never cryptic again: **pacs** = Payments Clearing and Settlement; **008** = the FI-to-FI customer credit transfer. Full technical id: `pacs.008.001.08` (the trailing numbers are variant and version).

It's strictly **bank-to-bank**. It is *not* what Bob's app sends (that's a pain.001). Bob's bank receives the pain.001 and *turns it into* a pacs.008 to pass onward. Created by the **Debtor Agent** (Bob's bank), received by the **Creditor Agent** (Sweety's bank) — directly or through intermediaries. Every hop preserves who pays whom.

{{embed:explorer:PACS.008|See pacs.008 in the Message Explorer}}

## The anatomy, block by block

Two big parts: a **Group Header** (facts about this hop) and one or more **Credit Transfer Transaction** blocks (the payment).

```xml
<GrpHdr>
  <MsgId>BNKAUS33-20260627-000400</MsgId>
  <CreDtTm>2026-06-27T09:30:00+04:00</CreDtTm>
  <NbOfTxs>1</NbOfTxs>
  <SttlmInf><SttlmMtd>INDA</SttlmMtd></SttlmInf>
</GrpHdr>
```

- **`MsgId`** — a reference for *this hop only*. It changes every forward. Point-to-point, disposable.
- **`CreDtTm`** — when the message was created, **with a UTC offset** (`+04:00`). Drop the offset and the receiver can't place it in time. A real rejection cause.
- **`NbOfTxs`** — how many transactions inside.
- **`SttlmMtd`** — *how* the banks settle: across accounts they hold with each other (the nostro/vostro from Level 100) or via a clearing system.

```xml
<CdtTrfTxInf>
  <PmtId>
    <EndToEndId>BOB-INV0042</EndToEndId>
    <UETR>eb6305c9-1f7c-4a9b-9b1e-2c2f4e7a91d4</UETR>
  </PmtId>
  <IntrBkSttlmAmt Ccy="USD">400.00</IntrBkSttlmAmt>
  <Dbtr><Nm>Bob Marsh</Nm></Dbtr>
  <DbtrAgt><FinInstnId><BICFI>EBILAEAD</BICFI></FinInstnId></DbtrAgt>
  <CdtrAgt><FinInstnId><BICFI>HDFCINBB</BICFI></FinInstnId></CdtrAgt>
  <Cdtr><Nm>Sweety Rao</Nm></Cdtr>
  <RmtInf><Ustrd>Invoice 0042 — June freelance</Ustrd></RmtInf>
</CdtTrfTxInf>
```

The cast you know, now in tags: **`Dbtr`/`Cdtr`** the Debtor (Bob) and Creditor (Sweety); **`DbtrAgt`/`CdtrAgt`** their banks, each a **BIC** of 8 or 11 characters; **`IntrBkSttlmAmt`** the amount that settles between the banks, currency as an attribute; **`RmtInf`/`Ustrd`** what the payment is *for*.

## The three identifiers everyone confuses

The single most common interview question on pacs.008 — commit it to memory:

- **`MsgId`** — point-to-point, rotates every hop, means nothing end to end.
- **`EndToEndId`** — the **customer's** reference, travels **unchanged** first bank to last.
- **`UETR`** — a globally unique **UUIDv4** identifying the *whole* payment across *every* bank; what powers "where is my payment?"

## Schema-valid, still rejected

{{think}}
Here's a pacs.008 that passes every XSD check — well-formed XML, all mandatory elements present. You send it to production and it gets NAK'd anyway. Before reading on: what are the usual culprits?
{{reveal}}
The failures that live *above* the schema, in the usage rules and the real world:

- **Placeholder references.** An `EndToEndId` of `NOTPROVIDED`, `NA`, or empty. It must be a real value, ≤35 chars, that survives every hop.
- **Missing timezone.** A `CreDtTm` with no UTC offset is ambiguous across borders and gets bounced.
- **Malformed UETR.** Uppercase letters or wrong version/variant bits, so it's no longer a valid UUIDv4.
- **Truncated names.** The old MT103 had a 35-char name field; clip a long name and sanctions screening flags the *wrong* party. ISO 20022 keeps the whole name — but only if the upstream system actually sends it.

Schema-valid and *accepted* are two different bars, and this gap is exactly where real payments die.
{{/think}}

The fastest way to make this stick: edit a real pacs.008 and watch it validate live. The Playground holds Bob's exact transfer — change a name to 40 characters, blank the reference, drop the timezone, and watch each one get caught. You can also run it through the live engine to see it become an MT103 and back.

{{embed:playground|Edit a live pacs.008 in the Playground}}

{{aside:model|The mental model}}
**pacs.008 is the interbank workhorse — the machine-readable "pay this customer, exactly, no ambiguity."** Every hop preserves who pays whom. `MsgId` rotates per hop; `EndToEndId` and `UETR` are the threads that survive the whole journey.
{{/aside}}

{{aside:chair|From the engineer's chair}}
The rejections that hurt aren't schema errors — they're usage-rule and real-world ones: placeholder `EndToEndId`, timezone-less `CreDtTm`, malformed `UETR`, truncated names. Validating against the raw XSD passes all four. Validate against the *usage guideline* (CBPR+/HVPS+), and remember the full id `pacs.008.001.08` — the version is pinned by the rulebook, not by you.
{{/aside}}

{{aside:breaks|Where it breaks}}
- **Placeholder `EndToEndId`** (`NOTPROVIDED`/`NA`/empty) — rejected; breaks the customer's thread.
- **`CreDtTm` with no UTC offset** — ambiguous across borders, bounced.
- **Malformed `UETR`** — uppercase or wrong variant bits, no longer a valid UUIDv4.
- **Truncated names** — clipped names trip sanctions screening on the wrong party.
{{/aside}}

{{aside:map|The map}}
The workhorse and its neighbours:

- Where it's born → {{link:article:302-pain-001|pain.001}}.
- The family it belongs to → {{link:article:302-pacs-family|the pacs family}}.
- Where its money actually settles → {{link:article:101-nostro-vostro|nostro & vostro}}.
{{/aside}}

{{aside:ref|Reference card}}
- **pacs.008** = FI-to-FI customer credit transfer, the MT103 successor. Strictly bank-to-bank.
- **Two parts:** Group Header (this hop) + Credit Transfer Transaction (the payment).
- **Ids:** `MsgId` (per hop) · `EndToEndId` (customer, end-to-end) · `UETR` (global tracking).
- **Real rejections:** placeholder EndToEndId · missing timezone · malformed UETR · truncated names.
- **Full id** `pacs.008.001.08` — version pinned by the usage guideline.
{{/aside}}

## So what can you do now?

You can open any pacs.008, point to the Group Header and the transaction, name the Debtor/Creditor and their agents, tell `MsgId` from `EndToEndId` from `UETR` without hesitating, and predict the handful of reasons a real payment gets rejected before it ever reaches Sweety.

{{check:pacs.008 is best described as…|The interbank workhorse that moves a customer's payment between banks|A customer's request to their own bank|An end-of-day account statement}}

{{check:Which reference stays with the payment across every bank, end to end?|The UETR — a globally unique tracking reference|The MsgId, which rotates at each hop|The sending bank's internal batch number}}
